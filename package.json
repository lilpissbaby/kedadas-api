{
  "name": "api-fiestas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API de fiestas locales geolocalizadas · Hono sobre Cloudflare Workers o Docker + MongoDB",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "construir": "node node/construir.mjs",
    "start": "node dist/node/servidor.js",
    "seed": "node scripts/seed.mjs",
    "indices": "node scripts/indices.mjs",
    "probar": "node scripts/probar.mjs",
    "bench:node": "node scripts/bench-node.mjs",
    "tipos": "tsc --noEmit",
    "secreto": "node -e \"console.log(require('crypto').randomBytes(48).toString('base64'))\""
  },
  "dependencies": {
    "hono": "^4.9.0",
    "mongodb": "^6.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
